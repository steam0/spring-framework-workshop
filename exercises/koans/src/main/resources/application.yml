spring:
  datasource:
    url: jdbc:h2:mem:workshop
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

orders:
  max-items: 10
  currency: NOK
  discount-percent: 0
